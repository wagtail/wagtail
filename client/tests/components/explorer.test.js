/*eslint-disable */
import '../stubs';
import { expect } from 'chai';

import Explorer from '../../src/components/explorer';


describe('Explorer', () => {
  it('exists', () => {
    expect(Explorer).to.exist;
  });
});
