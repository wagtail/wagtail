import comments from '../../../../../client/src/components/CommentApp/comments';

/**
 * Entry point loaded when the comments system is in use.
 */
// Expose module as a global.
window.comments = comments;