/*eslint-disable */
import { expect } from 'chai';
import Explorer from '../../src/components/Explorer';


describe('Explorer', () => {
  it('exists', () => {
    expect(Explorer).to.exist;
  });
});
